/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {BoardRow} from './board-row';

describe('BoardRow', () => {
  it('should create an instance', () => {
    expect(new BoardRow()).toBeTruthy();
  });
});
